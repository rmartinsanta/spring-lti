<div class="alert alert-warning" role="alert">
    Sólo los alumnos están autorizados a asignarse una nota, si quieres asignarte una nota a tí mismo accede con el rol alumno.
    <a href="https://docs.moodle.org/35/en/Switch_roles">Como hacerlo en Moodle </a>
</div>

<h3>Configuración de la actividad</h3>
<hr>

<form method="post" action="/end" class="form-check" style="padding: 20px;">
    {{#retryAllowed}}
        <input class="form-check-input" type="checkbox" checked name="allowRetry">
    {{/retryAllowed}}

    {{^retryAllowed}}
        <input class="form-check-input" type="checkbox" name="allowRetry">
    {{/retryAllowed}}

    <input name="secretKey" type="hidden" value="{{secretKey}}" >

    <label class="form-check-label" for="defaultCheck1">
        Permitir reintentos
    </label>
    <input class="btn btn-success" type="submit" value="Guardar configuración">
</form>

<hr>