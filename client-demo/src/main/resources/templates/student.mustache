<p>Como eres un alumno muy majo, puedes decidir qué nota quieres tener! </p>

{{#canSubmit}}
<form action="/end" method="POST" >
    <div class="input-group mb-3">
        <input type="number" min="0" max="1" step="0.001" name="score" class="form-control" placeholder="Introduce un número entre 0 y 1, por ej 0.875 equivaldría a un 8,75" aria-label="Introduce la nota que te gustaria tener" aria-describedby="basic-addon2">
        <div class="input-group-append">
            <input class="btn btn-outline-secondary" type="submit" value="Enviar nota"/>
        </div>
        <input type="hidden" name="secretKey" value="{{secretKey}}" >
    </div>
</form>
{{/canSubmit}}

{{^canSubmit}}
    <div class="alert alert-danger" role="alert">
        Lo siento, ya has mandado una nota y la configuración no permite reintentar la actividad. Contacta con tu profesor si crees que se trata de un error.
    </div>
{{/canSubmit}}
