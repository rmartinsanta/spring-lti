package rmartin.lti.demo_plugin;

/**
 * Each config property is stored as a (key --> value) pair, for example CAN_RETRY --> true/false
 * All config keys must be in this enum
 */
public enum ConfigKeys {
    CAN_RETRY,
    // ... other config options for your activity
}
